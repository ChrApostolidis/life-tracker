## Deployment secrets

The GitHub Actions `Deploy to Server` workflow expects these repository secrets:

- `DEPLOY_HOST` (or legacy `SERVER_IP`)
- `DEPLOY_USERNAME` (or legacy `SERVER_USER`)
- `DEPLOY_SSH_KEY` (or legacy `SSH_KEY`)